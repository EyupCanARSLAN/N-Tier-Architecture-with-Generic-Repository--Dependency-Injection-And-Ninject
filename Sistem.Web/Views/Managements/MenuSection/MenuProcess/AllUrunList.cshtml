@model IEnumerable<Sistem.Core.Data.MenuK.Product>
@{
    ViewBag.Title = "AllUrunList";
}
<h2>All Product List</h2>
<p>
    @Html.ActionLink("Create New", "CreateUrun")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName )
        </th>
        <th></th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete This Product In All Menu", "DeleteUrunInAllMenu", new {UrunId=item.ProductId})
        </td>
    </tr>
}
</table>
@Html.ActionLink("Back to List", "Index")